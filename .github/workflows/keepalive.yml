name: Keep Render app awake

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the app
        run: |
          curl -o /dev/null -s -w "HTTP %{http_code}\n" --max-time 90 "${{ secrets.RENDER_APP_URL }}"
